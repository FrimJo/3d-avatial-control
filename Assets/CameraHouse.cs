using UnityEngine;
using System.Collections;

public class CameraHouse : MonoBehaviour {

    public OVRCameraRig rig;

	// Use this for initialization
	void Start () {
	    
	}
	
	// Update is called once per frame
	void Update () {

        
    }

    // If the user moves the mouse in Y direction


    /*var wantedRotationAngle = transform.eulerAngles.y;
    var currentRotationAngle = rig.transform.eulerAngles.y;



    if (mouseY != 0)
    {
        // Rotate the house around the z axiz

        transform.Rotate(0.0f, 0.0f, mouseY);
    }*/
}
